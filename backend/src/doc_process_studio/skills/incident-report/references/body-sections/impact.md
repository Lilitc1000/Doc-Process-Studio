# Impact Section Reference

影响段建议拆为三个维度：
- `body_impact_scope`：影响范围（系统/服务/区域/用户群体）。
- `body_impact_severity`：严重级别（建议统一等级体系）。
- `body_business_impact`：业务影响，建议换行列出。

编写要求：
- 先给范围，再给等级，再给业务后果。
- 避免“影响很大”这类不可验证描述。
- 业务影响应能映射到实际业务行为（如下单失败、响应超时）。
