# Timeline Section Reference

时间线用于复盘关键过程，每个条目应包含：
- `time`：发生时间。
- `event`：该时刻发生的事件。
- `resolution`：采取的处理动作（如有）。
- `evidence`：监控、日志、告警或观察依据（如有）。

建议：
- 按时间顺序排列。
- 优先覆盖“发现 -> 缓解 -> 修复 -> 恢复验证”链路。
- `body_affected_date_summary` 与时间线起止时间保持一致。
