services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    privileged: true
    environment:
      - PYTHONUNBUFFERED=1
      - LOCAL_WORKSPACE_FOLDER=/workspace
    volumes:
      - /var/run/docker.sock:/var/run/docker-host.sock
      - ..:/workspace:delegated
      - ~/.codex:/home/vscode/.codex
    ports:
      - "5173:5173"
      - "8000:8000"
    command: bash -lc '/workspace/.devcontainer/startup.sh'
